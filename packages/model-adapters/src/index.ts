/**
 * @apprentice/model-adapters: replaceable VisionAgentProvider implementations
 * (mock, generic OpenAI-compatible, exact UI-Mate port) plus the UI-Mate
 * protocol port itself under `uimate/`.
 */
export * from "./providers/index.js";
export * as uimate from "./uimate/index.js";
